export interface User {
  login: string;
  user_name: string;
  name: string;
  email: string;
  provider: string;
  provider_icon: string;
  provider_link: string;
  avatar_url: string;
  role: string;
}
